dependencies:
  pre:
    - sudo pip install awsebcli --force-reinstall
deployment:
  production:
    branch: master
    commands:
      - mkdir .elasticbeanstalk
      - cp resources/circleci/eb/config.yml .elasticbeanstalk/config.yml
      - eb deploy --profile default